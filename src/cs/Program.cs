using System;

namespace Main {
  class Program {

    // static method
    /// <summary>
    /// Pitying yourself has terrible connotations, but it
    /// belongs to what it means to be a friend to oneself.
    /// : The School of Life
    /// </summary>
    /// <param name="args">Input arguments.</param>
    static void Main(string[] args) {
      Console.WindowLeft = 0;
      Console.WindowTop = Console.CursorTop;
      Console.ResetColor();
    }
  }
}
